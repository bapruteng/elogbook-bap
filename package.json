{
  "name": "elogbook-bap",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install xlsx jspdf jspdf-autotable axios && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.5"
  }
}
